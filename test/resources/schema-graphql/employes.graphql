type Employee {
  id: ID!
  email: String!
  firstName: String
  lastName: String
  login: String!
  password: String!
  workInfo: Work
}

type Work {
  id: ID!
  job: String
  salary: String
  employees: [Employee]
}

type Query {
  Employees: [Employee]
}
