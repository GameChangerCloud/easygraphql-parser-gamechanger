type Movie {
    id: ID
    title: String
    casting: [Actor] @hasIverse(field: Abstract)
}

type Abstract {
    id: ID
    title: String
    movie: Movie!
}

type Actor {
    id: ID
    name: String!
}